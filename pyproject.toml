[build-system]
requires = ["setuptools==80.9.0"]
build-backend = "setuptools.build_meta"

[project]
name = "docassemble.ALTimeline"
version = "0.0.1"
description = "A vertical timeline module built as part of the Document Assembly Line project."
readme = "README.md"
authors = [
    { name="Suffolk Legal Innovation and Technology Lab", email="litlab@suffolk.edu" },
]
dependencies = []
license = "MIT"
license-files = ["LICENSE*"]

[project.urls]
Homepage = "https://docassemble.org"

[tool.setuptools.packages.find]
where = ["."]
